# Secrets — NEVER commit
.env
.env.*
*.key

# Results are reproducible artifacts, not source (§4: results/ gitignored).
results/

# Lean toolchain + prebuilt mathlib oleans built by scripts/setup_lean.sh (~5GB;
# regenerated by the script, never committed). §6.3 Lean track.
.lean/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/

# Caches / model weights (never committed; regenerated/downloaded on demand).
.cache/
.mypy_cache/
.ruff_cache/
*.ckpt
*.safetensors
*.pt
*.gguf
models/
hf_cache/

# Phase 6 IP mode (results-only): the moat lives here when relocated (§18 6.7).
PRIVATE/

# OS / editor
.DS_Store
