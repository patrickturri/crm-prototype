# Secrets — NEVER commit
.env
.env.*
*.key

# Results are reproducible artifacts, not source (§4: results/ gitignored).
results/

# Lean toolchain + prebuilt mathlib oleans built by scripts/setup_lean.sh (~5GB;
# regenerated by the script, never committed). §6.3 Lean track.
.lean/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/

# OS / editor
.DS_Store
